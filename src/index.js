import React from 'react';
import ReactDOM from 'react-dom';
import App from './__app__/main';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
